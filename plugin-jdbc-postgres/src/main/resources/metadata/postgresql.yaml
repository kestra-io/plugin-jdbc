group: io.kestra.plugin.jdbc.postgresql
name: "postgresql"
title: "PostgreSQL"
description: "Tasks that connect to PostgreSQL via JDBC to run queries and loads."
body: "Provide the PostgreSQL JDBC URL, credentials, and SQL statements (query, batch, load, trigger) to read or write data; configure fetch type, fetch size, and output options to return rows or files from workflows."
videos: []
createdBy: "Kestra Core Team"
managedBy: "Kestra Core Team"
